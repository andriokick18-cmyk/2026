# Decisões importantes do H2BApply

> Registro permanente: data, decisão, motivo, impacto, como testar. Uma
> decisão só sai daqui por outra decisão registrada. Histórico técnico
> completo (versão a versão) está no `CLAUDE.md`.

| Data | Decisão | Motivo | Impacto | Como testar |
|---|---|---|---|---|
| 2026-09-11 | Login e cadastro por usuário + senha; o Google entra só para conectar o Gmail de envio (`gmail.send`). | O login pelo Google prendia o site ao limite de 100 usuários do OAuth não verificado e confundia conta com Gmail de envio. | Cadastro com e-mail verificado por código; Gmail conectado depois, com plano ativo. | Smoke: cadastro, login, senha, connect-send. |
| 2026-09-12 | Conta grátis não envia nada (0 manual, 0 automático). | Ordem do dono: "nenhum usuário vai ter envio grátis". | Gate 402 em /api/send e /api/auto/start; textos honestos. | Smoke v172. |
| 2026-09-13 | Gmails de envio por plano: grátis 0 · VIP/VIPro 1 · DoublePro 2 · admin 6. | Ordem do dono. | `getMaxSenders`. | Smoke v172/v206. |
| 2026-09-13 | Tabela nova de limites (VIP 100/0 · VIPro 100/100 · DoublePro 200/200) carimbada em `vip.limits` na ativação; quem pagou antes mantém o contrato antigo. | Contrato congelado: nenhum pagante perde nada. | `limitesDoPlanoNovo`, `limitsParaAtivacaoAdmin`. | Smoke v187. |
| 2026-09-13 | Planilhas se alimentam sozinhas do DOL (coleta, enriquecimento, frescor, mensais); H-2B do mês fica sempre em rascunho, H-2A publica sozinha acima do mínimo. | Ordem do dono. | `mod-planilhas.js`. | Smoke v174/v182 com feed falso. |
| 2026-09-14 | O site é só em português; sem seletor de idioma; dicionários EN/ES ficam guardados mas inativos. | Público 100% brasileiro; conta presa em inglês era bug. | `_curLang = 'pt'`, migração de boot. | Smoke v199. |
| 2026-09-18 | E-mail do empregador só viaja inteiro para plano ativo ou admin; sem plano vai mascarado com `hasEmail` preservado. | O e-mail é o ativo que o cliente paga; estava aberto a qualquer scraper. | `mascararEmail`, `podeVerEmailVaga`. | Smoke v182 lote 10. |
| 2026-09-18 | Radar de vagas é filtro salvo que mostra vagas novas ao abrir o site; nenhuma promessa de push. | Não existe canal de push; prometer aviso era mentira. | `registrarVagasNovasNoRadar`. | Smoke v189. |
| 2026-09-18 | Restaurar backup congela gravações até o reinício; `backup.json` (tokens em claro) nunca mais é escrito. | O restore era desfeito pelo SIGTERM; tokens em texto puro no disco. | `persist()` com freeze, `flushAll` genérico. | Smoke v191 (drill real). |
| 2026-09-18 | Comprovantes de pedido vivem no disco (`comprovantes/`), nunca na RAM dentro de `pedidos.json`. | Cada persist reserializava todos os comprovantes; risco de OOM. | `saveComprovante`/`loadComprovante`. | Smoke v192. |
| 2026-09-19 | Códigos de migração VIP do site antigo nascem só pelo painel admin; lista de clientes nunca entra no git. | Repositório público; dado pessoal em git é vazamento permanente. | `/api/admin/migracao-codigos`, `/api/vip/resgatar-codigo`; `vip.source = "migracao"` conta como plano pago para features, zero caixa. | Smoke v206. |
| 2026-09-19 | Master Command adotado como especificação permanente do produto (regra nº 1: o usuário nunca fica perdido; site não é governo nem afiliado ao SeasonalJobs; nunca prometer resultado; nunca inventar dado ou regra). | Ordem do dono. | `docs/H2BAPPLY_PRODUCT_RULES.md`, `docs/H2BAPPLY_UX_RULES.md`, `docs/H2BAPPLY_WAY.md`. | Auditoria profunda antes de qualquer alteração; plano em lotes aprovado pelo dono. |
| 2026-09-19 | Testes no site ao vivo (h2bapply-2026.onrender.com) são do dono; a IA trabalha no código e em servidores de teste com dados falsos; nunca envia candidatura real nem altera dado real para testar. | Acesso ao navegador com sessão real é do dono; proteção de dados reais. | Achados do dono entram em `docs/H2BAPPLY_LIVE_SYSTEM_AUDIT.md`. | — |
| 2026-09-19 | Vaga enriquecida UMA vez pelo ETA Case Number; sem robô de frescor; sem "status ativa/inativa/expirada" para o usuário. Substitui a parte da seção 8 do Master Command sobre reconferir vaga. | Decisão do dono: não reconsultar vaga já completa; o site não é o DOL. | Robô "Planilha sempre fresca" desligado; enriquecimento só em linha incompleta; página da vaga sem status. | Smoke: nenhum agendador chama frescor; enriquecimento nunca toca linha completa. |
| 2026-09-19 | Mensagem de confiança em destaque: "100% das vagas vêm do site oficial (SeasonalJobs.dol.gov)… viu uma vaga em outro lugar e quer saber se é golpe? pesquise no Envio Manual". | Decisão do dono: diferencial de confiança, no lugar de explicar a importação técnica. | Landing e FAQ (fase de implementação do Master Command). | Texto presente na landing e na FAQ; guarda no smoke. |
| 2026-09-19 | O arquivo diário do DOL pode não existir para a data de hoje (404): o robô usa o dia anterior, nunca finge sucesso e re-tenta em 1 hora quando não há arquivo. | Caso real no site ao vivo (sábado 19/09): robô H-2A preso em ⚠️ Erro. | `baixarFeedComFallback` em mod-planilhas.js (H-2A novas e coleta mensal). | Smoke v207 (feed falso com 404 de hoje / de todos os dias). |
| 2026-09-19 | Robôs de planilha (enriquecimento, vagas novas H-2A, H-2A/H-2B do mês) perdem TODO agendamento automático — passam a ser 100% manuais (clique na aba Planilhas & Robôs). | Ordem do dono: "eu resumi o programa todo para nao gastar... robos só precisa 2 vezes por ano quando entrar planilha nova. e nao mais que isso". O DOL só publica temporada nova poucas vezes por ano. | `iniciarAgendadores()` não registra nenhum timer (mod-planilhas.js); textos do admin.html atualizados. Rotas manuais (`enrich/start`, `sheet/coleta-start`, `sheet/h2a-bimestral-run`, `sheet/h2b-mensal-run`, `sheet/h2a-novas-run`) continuam intactas; o auto-enrich pós-upload também continua (é parte do mesmo clique). | Smoke v217: nenhum `setInterval`/`setTimeout` de robô de planilha registrado no boot; rotas manuais seguem funcionando. |
| 2026-09-20 | Reestruturação da página de Planos: nomes públicos novos (Manual/Turbo/Máximo no lugar de VIP/VIPro/DoublePro), preços/duração novos (30 ou 60 dias, com desconto no de 60) e limites novos (Manual 100 manual/0 auto R$150-270 · Turbo 100/100 R$300-540 · Máximo 50 manual/300 auto R$500-900), design com diamante SVG bandeira do Brasil, tabela comparativa, plano recomendado em destaque, selo de economia dinâmico e FAQ. | Ordem do dono: a explicação dos planos estava confusa e os nomes não diziam nada pra quem compra. | `NOME_PLANO_PUBLICO`/`PLAN_LIMITS_NEW`/`PLANO_PRECO_TAB` (mod-config.js/server.js) são a fonte única; CHAVES internas `vip`/`vipro`/`doublepro` continuam as mesmas de propósito (contrato congelado em `vip.limits` protege quem já pagou antes da mudança — nenhum pagante perde nada). Varredura completa em app.js/index.html/admin.html/tutorial/como-usar/h2bapply-funciona por qualquer nome/preço/limite antigo. | Smoke v218 (preços/limites/nomes/duração ponta a ponta pela aprovação real de pedido) + verificação visual Playwright (desktop/mobile). |
| 2026-09-20 | Sistema de códigos de migração VIP do site antigo (v206, 19/09/2026) removido por completo do site — não existe mais em lugar nenhum (banner na aba Planos, aba do painel admin, rotas de resgate/criação/exclusão, banco `migration_codes.json`). | Ordem do dono: "delete o sistema de código de migração de todo o site, não vai ter mais isso". | Removidos: `/api/vip/resgatar-codigo`, `/api/admin/migracao-codigos` (GET/POST) e `/api/admin/migracao-codigos/excluir` (server.js); `resgatarCodigoMigracao()` e as chaves `mig_*` do LANG_DICT nas 3 línguas (app.js); o `<details id="mig-card">` da aba Planos (index.html); a view/aba "Códigos de migração" inteira do painel, incluindo `loadMigracao`/`renderMigracao`/`migCriar`/`migImportar`/`migExcluir`/`migCopiarMsg` (admin.html). | Smoke: os ~20 checks do v206 saíram junto (a seção documentando a feature no CLAUDE.md fica como registro histórico — a decisão de remover é esta linha nova). Varredura confirmou zero referência restante em nenhum arquivo do repo. |
| 2026-09-20 | Badge de aquecimento do Gmail do admin (`_warmupBadgeHTML`) não mostra mais a fração "enviados/teto-do-dia-tier" (ex.: "276/15") — pro admin isso nunca foi o limite real (é `ADMIN_AUTO_DAILY_LIMIT_PER_SENDER=450`, mostrado à parte); o texto agora diz que é só preferência de rodízio, nunca trava. Label "VIP Infinito · Máximo" virou só "VIP Infinito" (e "Unlimited VIP" em EN) nas 3 línguas + index.html. | Achado na auditoria ao vivo do dono pós-v220: os dois textos eram sobra confusa de mecanismos/renames anteriores (aquecimento pensado só pro cliente; "· Máximo" era resíduo do rename "DoublePro"→"Máximo" do v218 colado num status que não tem relação com plano de cliente). | `_warmupBadgeHTML()` (app.js) ganha ramo `U.isAdmin`; `pf_vip_infinito` no `LANG_DICT` (3 línguas) e no fallback estático do index.html. | Smoke v221 (`npm test` 100% verde — sem teste novo dedicado, os existentes de badge/label continuam passando); `check-duplicates.js`/`check-xss-guard.js` sem achados. |
| 2026-09-20 | Título de vaga com prefixo "CÓDIGO SOC: " truncado NA FONTE do DOL (60 caracteres, largura de campo antigo) é completado no boot só quando o código tem grafia oficial VERIFICADA (fonte: BLS OES/O*NET); qualquer outro título com a mesma assinatura de corte só vira aviso no log — nunca é adivinhado. Histórico de candidaturas já enviadas nunca é tocado. | Achado do dono (8 candidaturas reais enviadas com o título "...Repair Worke" em vez de "...Repair Workers"); causa raiz confirmada como dado truncado na origem (DOL), não bug do nosso import. | `_selfHealTitulosTruncados()` (server.js), chamada de `loadSheets()` no mesmo ponto que `_selfHealCidades`; mapa `_SOC_TITULOS_OFICIAIS` só com códigos verificados contra fonte oficial. | Smoke v222: 2º boot com planilha de teste (1 código verificado, 1 não) confere que só o verificado é corrigido e ambos aparecem no log. |
| 2026-09-20 | Aba "Vagas ao Vivo" (busca em tempo real na API do DOL) removida por completo, servidor e front; o site passa a mostrar vaga SÓ a partir de planilha pré-coletada. | Ordem do dono: "não quero mais dados ao vivo do dol... a partir de agora só planilhas". | Removidos `/api/jobs`, `/api/sheet-detail`, `/api/sheet-batch`, `fetchDOL`/`normJob`/`fetchByCase` (server.js) e a aba/filtros/estado exclusivos dela em app.js/index.html (`JOBS`, `mkCard`, `_vfLive` e ~15 branches do motor de filtros). Robôs periódicos de coleta de planilha (mod-planilhas.js, feed ZIP) NÃO foram tocados — continuam sendo a única fonte de vaga do site. | Smoke v223 (722 checks); `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v84→v85. |
| 2026-09-20 | Modal "Filtrar vagas": "Limpar tudo" (painel × chip fora dele) sincroniza os dois estados; contagem ao vivo do painel e contagem do já-aplicado usam contadores de sequência separados (nunca mais uma descarta a resposta fresca da outra); busca que normaliza pra string vazia (só pontuação/apóstrofo) é ignorada em vez de devolver "0 vagas". | Auditoria manual real do dono (dezenas de combinações) achou os 3 bugs; nenhum era urgente, registrados e corrigidos. | `vfClearDraft`/`vfClear` (app.js) sincronizam VF.st↔VF.draft; `vfFetch` usa `VF.seq`/`VF.seqAplicado`; `searchSheet` (server.js) ganhou guarda `if(!ql)`. | Smoke v224 (6 checks novos); `' OR 1=1` testado direto por HTTP e em Chromium real sem reproduzir crash nenhum; sw.js v85→v86. |
| 2026-09-20 | Toda leitura de resposta do portão de autenticação (login/cadastro/recuperação de senha) passa por `_agReadJson()` — resposta não-JSON (HTML de erro do proxy durante deploy) vira mensagem honesta de "servidor reiniciando", nunca o erro técnico cru; texto da Central de Tutoriais corrigido pra não prometer foto em item que só tem texto. | Dono testou ao vivo logo depois de um deploy e achou o erro cru no login; leu a Central de Tutoriais e viu que só 6 de 20 itens têm foto. | `_agReadJson` (app.js), usada por `agSubmitLogin`/`agSubmitSignup`/`agEnviarCodigo`/`agConfirmarCodigo`/`agRecEnviar`/`agRecRedefinir`; chave `tut_s` (3 línguas) + fallback do index.html. | `npm test` 100% verde; sw.js v86→v87. |
| 2026-09-20 | Reuso de comprovante Pix (mesmo arquivo/transação em conta diferente) agora barra a ativação PROVISÓRIA AUTOMÁTICA, não só a aprovação manual do admin — inclui o caso de reuso contra um 1º pedido que só foi auto-ativado (status continua "pendente" pra sempre, nunca vira pago/ativo/cancelado sozinho). | Achado CRÍTICO de uma das 3 auditorias paralelas do v225: o comentário do código dizia que essa checagem já existia no caminho automático, mas nenhuma função ali olhava hash/transação — só o valor lido; o 2º furo (status "pendente" nunca batia no filtro antigo) apareceu escrevendo o próprio teste de regressão do fix. | `_comprovanteJaUsado(pd)` (server.js, função única) chamada pela aprovação manual e pelos 2 pontos de `preCheckComprovante` que chamam `autoAtivarProvisorio`; filtro de status ganhou `||x.autoAtivado`. | Smoke v226 (3 checks novos: 1ª ativação normal, 2ª bloqueada com preCheck ainda CONFERE, estrutural de ≥4 chamadas); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sem bump de sw.js (server.js only). |
| 2026-09-20 | `pauseAuto`/`resumeAuto`/`stopAuto` passam a checar `d.ok` (via `jsonSafe`) antes de mudar `U.autoJob`/UI — um 401/404 do servidor não marca mais "Pausado"/"Retomado"/"Parado" na tela quando nada mudou de verdade; `loadPlanos()` ganha retry automático (até 3x) antes de mostrar "Não deu pra carregar os planos". | Achado Alta de uma das 3 auditorias paralelas do v225 (pause/resume/stop) + achado do dono testando ao vivo (Planos mostrava erro na 1ª entrada, "Tentar de novo" resolvia na hora — mesma classe do bug de login do v225, só que na página mais importante do funil de pagamento). | `jsonSafe()` reusado (nenhum helper novo) em `pauseAuto`/`resumeAuto`/`stopAuto`; `loadPlanos()` (app.js) ganha loop `for(let _t=0;_t<3;_t++)` com pausa curta crescente. | Smoke: 2 checks estruturais novos (sem UI em Chromium nesta suíte); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v87→v88. |
| 2026-09-20 | Banir um Gmail (`/api/admin/ban-email` ou `banir:true` no delete-user) agora IMPEDE recadastro de verdade com o mesmo e-mail — antes `DB_BLOCKED.emails` era só decorativo (nenhuma rota de cadastro/login checava a lista). | Achado Alta de uma das 3 auditorias paralelas do v225: a mesma pessoa recriava conta na hora só trocando o username, já que a identidade de login não tem relação com o e-mail de contato banido. | `/api/cadastro` (server.js) ganha checagem `DB_BLOCKED.emails.includes(email)` logo após validar o formato Gmail, antes até do fluxo de verificação de código — 403 com WhatsApp de suporte. Escopo contido: `/api/login` e o OAuth de conectar Gmail de envio não foram tocados (fora do achado original). | Smoke: regressão ponta a ponta (ban → código confirmado → cadastro recusado 403 → unban → cadastro normal); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sem bump de sw.js (server.js only). |
| 2026-09-20 | `doClearHist()` (reset de Enviadas) passa a confirmar com o servidor ANTES de zerar o histórico local e mostrar "Resetado ✓" — antes a tela mudava e mentia mesmo quando o `fetch` de `/api/history/clear` falhava (escondido num `catch{}` vazio). | Achado Alta de uma das 3 auditorias paralelas do v225: uma falha real deixava o usuário achando que resetou, e a próxima tentativa de reenviar manualmente batia sem aviso no bloqueio de duplicata de verdade (regra 8). | `doClearHist()` (app.js) inverteu a ordem — `jsonSafe`+`d.ok` primeiro, mutação local depois — e passou a devolver `true`/`false`; `doResetAuto()` só mostra a dica de próximo passo quando o retorno é `true`. | Smoke: 2 checks estruturais novos (ordem fetch-antes-mutação; `doResetAuto` condicionado); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v88→v89. |
| 2026-09-20 | Escolher um remetente manual BLOQUEADO pelo Google agora é recusado com 409 claro em vez de mandar pelo Gmail principal em silêncio; o dropdown "Enviar por" também parou de listar conta bloqueada como opção. | Achado Média de uma das 3 auditorias paralelas do v225: `getSenderToken` só filtrava `blocked` no pool do round-robin automático, não no ramo de remetente explícito do envio manual — o `catch` genérico trocava pro principal sem avisar. | `getSenderToken` (server.js) lança `SENDER_BLOCKED` (mesmo padrão do `WARMUP_CAP_REACHED`) e `/api/send` devolve 409 `senderBlocked:true`; dropdown do modal de envio (app.js) ganhou `&&!x.blocked`. | Smoke: 1 check estrutural + 1 comportamental ponta a ponta com Gmail falso (zero e-mails saem quando o remetente escolhido está bloqueado); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v89→v90. |
| 2026-09-20 | 4 lugares que buscavam perfis frescos do servidor (Currículos, painel do Automático, abrir Automático, parar Automático) unificados em `_refreshProfiles()` — só aceita a resposta quando `profiles` é um Array de verdade; um 401 de sessão caída nunca mais é tratado como "0 perfis", e `renderProfiles()` ganhou o fallback pra `U.profiles` que já existia em ~10 outros lugares do arquivo. | Achado do dono testando ao vivo: conta com 326 candidaturas automáticas mostrava badge "Currículos 1" mas o corpo dizia "Nenhum perfil criado ainda" — mesma família dos bugs de carregamento já corrigidos em Planos e login. | `_refreshProfiles()` (app.js) fonte única; `loadProfilesView`/`_renderAutoProfilesPanel`/`openAutoModal`/`stopAuto` passam a chamá-la; `renderProfiles()` auto-cura `UPROFILES` a partir de `U.profiles` antes do estado vazio. | Smoke: 3 checks estruturais (sem Chromium nesta suíte); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v90→v91. |
| 2026-09-20 | `deleteProfile()` passa a checar `d.ok` (via `jsonSafe`) antes de remover o card da tela — a recusa do servidor de apagar o último perfil restante (400) não finge mais sucesso; o perfil não reaparece "sozinho" depois. | Achado Média de uma das 3 auditorias paralelas do v225: a resposta do `/api/profiles/delete` era descartada por completo. | `deleteProfile()` (app.js) segue o mesmo padrão de `pauseAuto`/`doClearHist` (v227/v229): `jsonSafe`+`d.ok` antes da mutação otimista. | Smoke: 1 check estrutural (ordem checagem-antes-mutação); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v91→v92. |
| 2026-09-20 | `GET /api/debug` (total de usuários, sessões ativas, jobs automáticos rodando, caminho do disco) exige admin agora — antes era público, sem nenhuma checagem de sessão. | Achado Média de uma das 3 auditorias paralelas do v225: varredura confirmou zero telas do site chamando essa rota — nenhum uso legítimo público pra justificar. | server.js: mesma trava da rota irmã `/api/debug/export` (`isAdminEmail(_sessAdminEmail(s))`, o padrão mais comum do arquivo pra admin hardcoded). | Smoke: 2 checks comportamentais (sem sessão → 403; admin → 200); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sem bump de sw.js (server.js only). |
| 2026-09-20 | `POST /api/pedido/:id/comprovante` (reenvio de comprovante) ganha rate limit de 5/hora por usuário — cada chamada grava ~8MB no disco e dispara nova leitura de IA (Gemini), sem trava nenhuma antes. | Achado Média de uma das 3 auditorias paralelas do v225: um script hostil ou um bug de retry em loop conseguia martelar a rota e gastar disco/cota de IA à toa. | server.js: `rateLimit("comprovante_reenvio_"+s.user_email,5,3_600_000)`, mesmo helper já usado em `/api/cadastro`/`/api/login`, chave por e-mail (não IP). | Smoke: regressão comportamental (6ª tentativa seguida no mesmo pedido leva 429); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sem bump de sw.js (server.js only). |
| 2026-09-20 | Removido `"SEM_COMPROVANTE"` do allowlist de leitura ruim na aprovação de pedido — veredito que nunca era produzido por nenhum código real (código morto). Fecha os 10 achados confirmados das 3 auditorias paralelas do v225. | Achado Baixa: `_geminiComprovanteParse`/gancho de teste só produzem ERRO/ILEGIVEL/CONFERE/DIVERGENCIA; pedido sem comprovante já ficava com `preCheck` null, coberto pelo `_pc&&[...]`. | server.js: array reduzido pra `["DIVERGENCIA","ILEGIVEL"]`, zero mudança de comportamento observável. | Smoke: guarda estrutural permanente (a string não pode mais existir no server.js); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sem bump de sw.js (server.js only). |
| 2026-09-20 | `checkStatus()` (boot do site inteiro) tenta buscar `/api/status` até 3x antes de mostrar a landing — antes, 1 resposta não-JSON (deploy reiniciando) deslogava na hora um usuário pagante com sessão válida. | Achado Crítica de uma 2ª rodada de 3 auditorias paralelas: era o único ponto de entrada do app sem a proteção já dada às 6 funções de auth (v225) e ao `loadPlanos` (v227b). | `checkStatus()` (app.js) ganha o mesmo loop `for(let _t=0;_t<3;_t++)` com `jsonSafe`, ANTES do `if(d.connected)`; resposta legítima "não conectado" não é retentada. | Smoke: 1 check estrutural (loop de retry presente); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v92→v93. |
| 2026-09-20 | `/api/admin/user/full-update` (2ª porta pro plano de um cliente, sem `addCredito`/`logAdminAction`/trava, campos de pagamento 100% decorativos) e `/api/admin/push-user` (código morto, `ok:true` sem efeito nenhum) REMOVIDAS; `set-user-field` (campo `isAdmin`) e `vip/set-expiry` passam a exigir/usar a mesma trava de admin-hardcoded e de duplo-clique que o resto das rotas de VIP já tinha; +4 `jsonSafe` (`doSend`/`saveProfile`/`_manualCdSave`/`_removeSenderEmail`) fecham o resto do `r.json()` cru do app.js. | Resto dos achados da 2ª rodada de 3 auditorias paralelas (painel-admin/dinheiro) que o v236 não cobriu; zero chamador confirmado por grep nas 2 rotas removidas. | server.js: `full-update`/`push-user` deletadas com comentário explicando o porquê; `set-user-field` ganha `isAdminEmail(_sessAdminEmail(s))` + `logAdminAction` no campo `isAdmin`; `vip/set-expiry` ganha `_adminVipActivateLock` (mesmo padrão do `vip/activate`/`set-plan`). app.js: 4 `jsonSafe(r)`. | Smoke: 7 checks novos (3 estruturais, 4 comportamentais — recusa/permissão do admin hardcoded + audit trail, 1º clique ok/2º clique 409); `npm test` 100% verde (751 checks); `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v93→v94. |
| 2026-09-20 | `loadSheetMeta()` (busca manual de vagas) ganha contador de contexto (`sCtxSeq`) — trocar de aba/busca/ordenação/filtro nunca mais fica preso atrás de um fetch antigo em voo, e a resposta desse fetch velho (que podia misturar vagas da aba ERRADA na lista atual) é descartada em silêncio quando chega. | Último achado Alta da 2ª rodada de 3 auditorias paralelas (vagas/filtros) — mesma classe do bug já corrigido em `vfFetch` no v224, só que aqui não tinha proteção nenhuma. | `loadSheetMeta()` (app.js): `reset=true` bypassa o gate de `sLoading` e incrementa `sCtxSeq`; `mySeq` capturado no início é comparado ao `sCtxSeq` atual no sucesso, no catch e no finally; ganhou `jsonSafe` de brinde. | Smoke: 1 check estrutural (todos os elementos do padrão presentes); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v94→v95. |
| 2026-09-20 | `deleteCvFromAccount()` varre `UPROFILES`/`U.profiles` limpando `resumeIdx`/`coverIdx`/`pdfName`/`coverName` de QUALQUER perfil que apontava pro PDF apagado — antes só o perfil sendo editado era limpo no cliente, e um perfil diferente mostrava currículo "disponível" que o servidor já tinha apagado (via a injeção de DOCS fantasma do `openModal`). | Último achado Alta da 2ª rodada de 3 auditorias paralelas (perfil/onboarding) — fecha essa rodada por completo. `/api/cv/delete` já fazia essa limpeza no servidor pra todos os perfis; faltava espelhar no cliente. | `deleteCvFromAccount()` (app.js) ganha a mesma varredura que o servidor já faz + `jsonSafe`. | Smoke: 1 check estrutural (varredura dos 2 arrays + `jsonSafe` presentes); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v95→v96. |
| 2026-09-20 | `mod-filtros.js`: família do cargo (agrupa grafias do mesmo cargo num chip) parava de cair no título quando o SOC era "sujo" (só pontuação, normaliza pra vazio) — concatenação de string (`"soc:"+_famNorm(r.soc)`) nunca é falsy, então `\|\| _famNorm(t)` nunca disparava; SOC sujo virava a família fantasma `"soc:"`, misturando cargos DIFERENTES no mesmo chip. | Último achado (Baixa, dormente) da 2ª rodada de 3 auditorias paralelas (vagas/filtros) — feeds do DOL sempre mandaram SOC bem-formado até agora, mas nada impede um feed sujo no futuro. | `mod-filtros.js`: `_famNorm(r.soc)` calculado antes, prefixo `"soc:"` só se não-vazio — mesmo padrão defensivo que `_famKey()` já usava. | Smoke: 1 check comportamental (2 títulos diferentes + SOC sujo idêntico → 2 chips, nunca `"soc:"`); confirmado que o teste falha contra o código antigo; `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sem bump de sw.js (server-side only). |
| 2026-09-20 | `saveProfileFromEditor()` injeta o PDF/cover recém-enviado em `DOCS` na hora — antes só `UPROFILES` era atualizado, e subir um currículo pra um perfil e tentar vincular esse MESMO arquivo em outro perfil logo em seguida não achava a opção até recarregar a página. | Achado Média (perfil/onboarding) que tinha ficado pendente da 2ª rodada; fecha os achados Alta/Média das 3 auditorias paralelas por completo. | `saveProfileFromEditor()` (app.js): `DOCS.push({idx,name,size,cvType})` com os dados que o `/api/cv/upload` acabou de devolver, guardado contra duplicata. | Smoke: 1 check estrutural (push em `DOCS` nos 2 uploads); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v96→v97. |
| 2026-09-20 | `startAuto()` e `wppRequiredSave()` passam a exigir ≥10 dígitos de WhatsApp (mesma régua do cadastro, "com DDD") — antes aceitavam ≥8, deixando passar número sem DDD que nunca funcionava de verdade pro empregador chamar. | Último achado (Baixa) da 2ª rodada de 3 auditorias paralelas (perfil/onboarding) — fecha TODOS os achados reais das 3 auditorias. | `app.js`: os 2 limites `<8` viram `<10`, mesma mensagem de erro do cadastro; `wppRequiredSave()` ganha `jsonSafe`. `/api/settings`/`saveProfile()` não mexidos de propósito (padrão já existente do arquivo: `phone` também sem mínimo nessa rota genérica). | Smoke: 1 check estrutural (as 2 réguas em ≥10, `jsonSafe` presente); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v97→v98. |
| 2026-09-20 | `#auth-gate` (login/cadastro/recuperação da landing) ganha 3 jeitos de fechar — botão X, clique fora do card, tecla Escape — e a seta ← ganha mais contraste. Antes não tinha NENHUM (só a seta, que só troca de tela interna, nunca fecha). | Achado Alta de uma 3ª rodada de auditoria (área auth); confirmado ao vivo pelo dono testando ("parece tela sem saída" sem reparar na seta pequena). | `closeAuthGate()` (app.js, nova) remove `open`/`ag-lock`; `.ag-close` no index.html com o mesmo padrão `event.target===this` de todo outro overlay; Escape reaproveita o listener global do `#vf-overlay`. `.ag-back`/`.ag-close` com contraste maior. | Smoke: 2 checks estruturais (função remove as 2 classes certas; os 3 fechamentos presentes); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sw.js v98→v99. |
| 2026-09-20 | `/api/login` bem-sucedido numa conta com `accountDeleted:true` agora limpa esse campo de verdade — antes o login funcionava normal (a senha nunca é apagada no soft-delete) mas o automático continuava quebrado pra sempre em silêncio, sem o front nunca mostrar isso. | Achado Alta de uma 3ª rodada de auditoria (área auth): o próprio comentário do `/api/account/delete` promete "relogar restaura tudo" mas nenhum código cumpria. | `/api/login` (server.js): `if(u.accountDeleted){setUser(...,{accountDeleted:false,deletedAt:null});addLog(...);trackJourney(...)}` antes de criar a sessão. | Smoke: 2 checks comportamentais lendo `/api/admin/live` (estado em MEMÓRIA — `accountDeleted` não é campo crítico, `users.json` em disco grava debounced 5s e daria falso-negativo lido direto); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sem bump de sw.js (server-side only). |
| 2026-09-20 | Os 3 watchdogs de recuperação do automático (orphan_recovery, guard de "sending" preso, daily-reset) ganham try/catch por iteração — 1 job com dado malformado não trava mais a recuperação de TODOS os outros usuários, em todo ciclo, pra sempre. | Achado Alta de uma 3ª rodada de auditoria (área gmail-envio): só o loop de diagnoseJob() já tinha essa proteção; os outros 3 iteravam Object.entries(DB_AUTO) sem nada, e uma exceção síncrona interrompia o for-loop naquele ponto — como a ordem é estável, o mesmo usuário quebraria todo ciclo seguinte. | server.js: os 3 `for(const[email,job] of Object.entries(DB_AUTO))` ganham `try{...}catch(e){console.error(...)}` por iteração, mesmo padrão do diagnoseJob(). | Smoke: 1 check estrutural (os 3 try/catch presentes); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sem bump de sw.js (server-side only). |
| 2026-09-20 | `getSenderToken()` relê `getUser()` antes de CADA escrita de `tokenExpired` (branch manual, round-robin automático, e dentro de `refreshSenderToken()`) — antes reusava um `p` capturado no topo da função; quando 2 senders falhavam na MESMA chamada, a 2ª escrita apagava a marcação que a 1ª tinha acabado de gravar. | Achado Alta de uma 3ª rodada de auditoria (gmail-envio): só o ÚLTIMO sender que falhava num ciclo ficava com RECONECTAR correto; os anteriores voltavam a parecer saudáveis e o robô tentava enviar por eles de novo, desperdiçando o slot de ~7min. | server.js: os 3 pontos de escrita trocam `p.senderEmails` por `getUser(ownerEmail)?.senderEmails` na hora de gravar. | Smoke: 1 check comportamental REAL (admin de teste com 2 extras de refresh_token morto, rodízio restrito a só eles via `senders`, confere que os 2 terminam `tokenExpired:true`); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sem bump de sw.js (server-side only). |

| 2026-09-20 | `POST /api/admin/vip/gift-days` (dias de cortesia) ganha a trava `_adminVipActivateLock` (409 no 2º clique em 5s) e `logAdminAction` — antes era a ÚNICA das 4 rotas que mexem em `vip.manualExpires`/`autoExpires` sem nenhuma das duas proteções que as rotas irmãs já tinham. | Achado Alta de uma 3ª rodada de auditoria (admin): duplo-clique/retry somava a cortesia 2x em silêncio (mesma classe do "caso Cleiton", 13r) e a concessão nunca aparecia em `/api/admin/audit` — não dava pra ver quem deu quantos dias pra quem, nem reverter pelo ↩️. | server.js: trava `_giftKey=admin+"\|"+email` (TTL 5s) logo após validar o motivo; `logAdminAction(admin,"vip_gift_days",email,snapshotAntes,snapshotDepois,motivo)` após o `setUser`. | Smoke: 3 checks comportamentais REAIS (1º clique concede normal; 2º clique em seguida no mesmo usuário é 409 `duplicate:true`; `/api/admin/audit` mostra a entrada `vip_gift_days`); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sem bump de sw.js (server-side only). |
| 2026-09-20 | `/api/send` (manual) para de reenviar cegamente pelo Gmail principal quando o sender ESCOLHIDO (extra explícito ou round-robin) falha por timeout AMBÍGUO — `httpsReq` (mod-gmail.js) marca `err.noResponse=true` nas 2 rejeições sem resposta HTTP (socket/timeout 15s); os 2 pontos de fallback do envio manual checam essa marca e devolvem 502 `ambiguousSend:true` em vez de reenviar. | Achado Alta de uma 3ª rodada de auditoria (gmail-envio): sem resposta do Gmail não dá pra saber se o envio já saiu — reenviar por OUTRA conta arriscava mandar a MESMA candidatura 2x pro empregador. Regra 13d/13e (gmail.send-only, nunca lê inbox) impede confirmar via API — a única correção segura é nunca reenviar automaticamente quando é ambíguo. | mod-gmail.js: `httpsReq` marca `err.noResponse` nas rejeições sem resposta. server.js: os 2 catches do fallback manual (`e2`/`e3`) checam `.noResponse` antes de cair pro principal. | Smoke: 2 checks comportamentais reais (networkError forçado no fake Gmail → 502 ambiguousSend, zero e-mails saem; contraste com erro CONFIRMADO HTTP 500 → fallback normal continua enviando) + 1 estrutural (mesma trava nos 2 catches); `npm test` 100% verde; `check-duplicates.js`/`check-xss-guard.js` sem achados; sem bump de sw.js (server-side only). |
